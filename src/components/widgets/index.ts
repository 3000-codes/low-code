import Circle, { defaultProps as CircleProps } from './Circle'

const Widgets = [
  {
    name: 'Circle',
    component: Circle,
    props: CircleProps,
    icon: '24gl-circle'
  }
]

export default Widgets
